# Empty = same-origin /api via Vite proxy (see vite.config.ts).
# Keeps session cookies first-party for both localhost and 127.0.0.1.
# Do not set to http://127.0.0.1:5000 — that breaks auth when the SPA is opened on localhost.
VITE_API_BASE_URL=
